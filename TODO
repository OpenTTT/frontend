Copied table styling in 4(!) places
Move services to service package instead of cross-importing feature modules just to get the services!
New order control shouldn't really be part of the orders FormArray, but how do we reconcile that with the table generation?
